# Hyphae

Hyphae are the connection paths between nodes.

Primary flows:
- `app/` -> `server/` via `/api/trpc`
- Dashboard -> Pulse (`:8765`)
- Dashboard -> Gateway (`:18789`)

See `lattice/topology.json` for the canonical map.
