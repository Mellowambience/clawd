# Nodes

Nodes are active agents or services.

Key nodes (source paths):
- `mycelium/` - Pulse + lattice emission
- `moltbot/` - Gateway bridge
- `app/` - Expo UI surface
- `server/` - tRPC API
- `scripts/` - Operator tools and agents

See `lattice/topology.json` for the canonical map.
