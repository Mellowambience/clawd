# Mycelium Dashboard ? Start Ritual

## Run
1. Start the pulse server:
   ```bash
   python mycelium_pulse.py
   ```
2. Open `http://127.0.0.1:8765/dashboard` in your browser.

## Incantation Trigger
Say in Clawdbot chat:
> "mycelium glow"

## Extend Later
- Replace `read_heartbeat()` with real log parsers
- Add specific Aurelia petals file(s)
- Expand node list or edge weights for richer resonance mapping

## Reliability Gate
Run the one-command ship gate before publishing:
```bash
python mycelium/ship_gate.py
```
This gate now includes:
- Python + dashboard JS syntax checks
- Companion local-action regressions
- 10-case hallucination eval suite
- Guardrail logging tests
- Frontend guardrail flow test (`node --test`)

Local-only. Close tab = gone.
